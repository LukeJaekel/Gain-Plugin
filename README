This is a simple gain plugin created uaing the JUCE framework.

Features:

- Changes volume of input (range is -40dB to 0dB)
- Stores last perameters set by user on closing Digital Audio Workstation (DAW)
- Includes a audio ramp feature that prevents unwanted audio clicks and artifacts
